# proxim
Proxim is a Python middleware for multi-agent systems that assigns every agent a cryptographic identity, attaches a signed certificate to each output, and maintains a reputation score on a shared ledger so agents can verify each other's outputs before acting on them.
